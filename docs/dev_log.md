# 开发日志

- 2026-06-13 23:32 - 阅读 `PCIe-6921` 上位机项目源码，梳理 GUI、采集线程、DLL 封装层、保存链路与 Time-Space 数据流。
- 2026-06-13 23:32 - 对比 `PCIe6921` 与 `PCIe6921` 的 API 头文件、DLL 导出表、接口说明 PDF 与调用流程图，识别时钟源编码、触发方向、速率模型、点数约束、解调通道配置等关键差异。
- 2026-06-13 23:32 - 编写 `PCIe6921和6921 API调用流程和方式对比.md`，重点整理 `6921` 的 API 调用顺序、参数语义、迁移风险与对 `7821` 现有工程的影响。
- 2026-06-13 23:32 - 编写 `PCIe6921上位机开发方案.md`，形成后续编码前的架构复用策略、模块改造范围、参数映射方案、实施步骤与测试计划。
- 2026-06-13 23:32 - 执行 UTF-8 与中文内容自检，确认文档中未出现乱码占位符。
- 2026-06-14 - 以 `PCIe-7821` 上位机为稳定基线建立 `PCIe-6921` 工程，复用 GUI、采集线程、异步保存、频谱、Time-Space 和 TCP 模块。
- 2026-06-14 - 引入指定的 64 位 `pcie6921_api.dll`，重写 ctypes 原型与设备调用接口，增加 `set_demodulation_ch_quantity`、`set_data_src`、`set_upload_rate`，移除不存在的 `set_trig_dir` 与 `set_upload_data_param`。
- 2026-06-14 - 完成 6921 专属参数模型和校验：时钟源编码反转、Raw 固定双通道、解调仅支持 1/2 通道、上传速率统一为 `1~5`、Phase 差分阶数限制为 `1~8`、脉冲宽度限制为 `4 ns` 整数倍。
- 2026-06-16 - 复核现场需求后修正 2026-06-14 的 Raw 约束理解：Raw 上传通道数允许 `1` 或 `2`，不再在 GUI 中强制锁定为 `2`。
- 2026-06-14 - 完成无卡仿真 GUI 初始化、Raw/Phase 参数联动、DLL 加载与导出函数绑定、4 KB DMA 对齐验证、模块导入和 Python 编译检查。
- 2026-06-14 - 新增参数约束和 DMA 对齐自动化测试，重写项目 README，并新增 `2026-6-14-6921上位机开发日志.md` 记录架构、数据流、问题与解决方案。
- 2026-06-14 - 收紧 DLL 边界层：配置与缓冲查询接口统一检查返回码；Raw/Phase 短读时只向上层返回 DLL 实际写入区域，避免旧缓冲数据混入。
- 2026-06-14 - 扩大中文乱码自检范围，修复主窗口中 3 处历史编码错配注释；严格 UTF-8 检查 26 个文本文件，未发现替换字符、问号占位或已知 mojibake 片段。
- 2026-06-14 - 增加旧参数文件兼容校正：当恢复的 Phase `MergePointNum` 不能整除 `Points` 时自动重置为合法默认值，验证 Raw/Phase 模式切换均可通过启动校验。
- 2026-06-16 - 修复 Raw、PSD 与 Time-Space 显示问题：Raw 模式放开 `1/2` 通道选择并按通道数执行点数约束；Raw 频谱采样率改为 `250 MHz / upload_rate_code` 对应的实际上传采样率；Phase 双通道 Time/Space 显示路径补齐 PSD 更新；Time-Space 滚动缓冲改为显式拷贝移位，避免重叠切片赋值导致历史列污染；补充单元测试并完成 UTF-8 中文自检。
- 2026-06-17 - 阅读 `logs/20260617_005055.log` 夜间运行日志，按本地硬盘存满前的有效区间完成稳定性分析，编写 `2026-6-17 运行一晚上测试日志分析报告与问题修复方案.md`，明确慢写盘、慢查询、慢读尖峰、保存统计日志口径和磁盘容量预警等问题及修复优先级；未修改业务代码。
- 2026-06-17 - 修复夜间运行日志分析报告中 KaTeX 公式转义问题，替换被 Python 字符串转义破坏的 `\text`、`\frac`、`\approx` 等公式命令，并重新执行 UTF-8 与中文乱码自检。
- 2026-06-17 - 修复夜间运行日志暴露的保存与采集诊断问题：优化写盘吞吐告警和分位数统计，归并慢查询日志口径，新增慢读统计，修正按帧切分保存器停止统计口径，并将分析文档重命名为 `2026-6-17 运行一晚上测试日志分析报告与问题修复日志.md`。
